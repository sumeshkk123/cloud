"use client";

/**
 * Shared popup used by hero sections across the site (e.g. MLM software modules,
 * services, contact). Use this so all hero sections open the same modal.
 */
export {
  ProjectBriefModal as HeroSectionPopup,
  type ProjectBriefModalProps as HeroSectionPopupProps,
} from "./project-brief-modal";
